for (let i = 0; i <= 5; i++) ((j) => setTimeout(() => console.log(j), j * 1000))(i)


const makeCounter = step => {
    let count = 0;

    return () => {
        const tmp = count;
        count += step;
        return tmp
    }
};
let a = makeCounter(5);
let b = makeCounter(3);
console.log(a());


const foo = n => m => n + m

const makeCounter = step => {
  let count = 0
  
  return () => (count += step)
}
